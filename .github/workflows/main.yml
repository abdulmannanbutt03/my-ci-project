name: First Pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Show files
        run: ls

      - name: Print message
        run: echo "Pipeline is working"
